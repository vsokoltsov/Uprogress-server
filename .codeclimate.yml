engines:
  rubocop:
    enabled: true
  brakeman:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  bundler-audit:
    enabled: true
  fixme:
    enabled: true
ratings:
  paths:
    - Gemfile.lock
    - app/**
    - "**.rb"
exclude_paths:
  - db/
  - "**/vendor/**/*"
